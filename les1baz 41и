#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a, b;
    cout << "Введите a" << endl;
    cin >> a;
    b = a*a*a;
    b = b*b*a; 
    a = b*b*b;
    cout << "a^21 = " << a << endl;
    return 0;
}
