#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    float a, y, m, n;
    cout << "Введите значение а" << endl;
    cin >> a;
    m = a*a + 1;
    n = sqrt(a*a+1);
    y = m/n;
    cout << "Ответ: " << y << endl;
    return 0;
}
