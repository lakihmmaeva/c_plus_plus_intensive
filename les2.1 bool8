#include <iostream>
using namespace std;

int main() {
    int a, b;
    cout << "Введите числа A и B: ";
    cin >> a >> b;
    bool result = (a % 2 != 0) && (b % 2 != 0);
    cout << boolalpha << result << endl;
    return 0;
}
