#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cout << "Введите стороны треугольника a, b, c: ";
    cin >> a >> b >> c;

    bool result = (a + b > c) || (a + c > b) || (b + c > a);
    
    cout << boolalpha << result << endl;
    return 0;
}
