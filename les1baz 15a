#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    float a, x, m, n;
    cout << "Введите значение а" << endl;
    cin >> a;
    m = 2*a + sin(abs(3*a));
    n = 3.56;
    x = sqrt(m/n);
    cout << "Ответ: " << x << endl;
    return 0;
}
