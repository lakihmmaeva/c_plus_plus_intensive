#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    float a, b, arif;
    cout << "Введите a" << endl;
    cin >> a;
    cout << "Введите b" << endl;
    cin >> b;
    arif = (a+b)/2;
    cout << "Среднее арифметическое чисел a и b равно " << arif << endl;
    return 0;
}
