#include <iostream>
using namespace std;
int main()
{
    int x, y, arif;
    cout << "Введите значение x" << endl;
    cin >> x;
    cout << "Введите значение y" << endl;
    cin >> y;
    arif = (x+y)/2;
    cout << "Среднее арифметическое числе a и b равно: " << arif << endl; 
    return 0;
}
