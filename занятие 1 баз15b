#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    float x, y, m, n;
    cout << "Введите значение x" << endl;
    cin >> x;
    m = 3.2 + sqrt(1+x);
    n = abs(5*x);
    y = sin(m/n);
    cout << "Ответ: " << y << endl;
    return 0;
}
